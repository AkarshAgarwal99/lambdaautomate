exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Hello froasfn,saf jsm myLambdaFunc final'),
    }
    return response
}
